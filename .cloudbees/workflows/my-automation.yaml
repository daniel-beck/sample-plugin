apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation

on:
  push:
    branches:
      - '**'

jobs:
  build:
    steps:
      - name: Build
        uses: docker://maven:latest
        shell: sh
        run: |
          echo "mvn clean verify"

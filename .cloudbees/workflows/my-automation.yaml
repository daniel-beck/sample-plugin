apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - cloudbees.io
jobs:
  build:
    steps:
      - uses: cloudbees-io/checkout@v1
      - uses: docker://maven:3.9.6-eclipse-temurin-11
        name: Build
